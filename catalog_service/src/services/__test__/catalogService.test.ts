describe("catalogService", () => {
  test("Example Test", () => {
    const a = 10;
    expect(a).toEqual(10);
  });
  test("Example Test 2", () => {
    const a = "Hello";
    expect(a).toEqual(a)
  });
});
